import React from 'react';
import ReactLoading from 'react-loading';
const Loading = () => {
    return (
        <div className='loading1'>
              <ReactLoading type='cylon' color='#535151' height={'10%'} width={'10%'} /> 
        </div>
    );
}

export default Loading;
